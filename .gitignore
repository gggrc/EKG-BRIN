# ===== Dataset besar (JANGAN di-push — unduh/buat ulang lokal) =====
ptb-xl-a-large-publicly-available-electrocardiography-dataset-1.0.3/
training/dataset/
training/classify/cache/

# ===== Model redundan / artefak latih =====
# (model yang DIPAKAI: unet_best.pt, ecgnet_best.pt, norm_stats.npz -> di-track)
training/checkpoints/unet_last.pt
training/checkpoints/history.npy
*.tmp
# Model tak-terpakai (USE_STRIP=False) / redundan / besar -> tidak di-push
training/checkpoints/strip_best.pt
training/checkpoints/strip_r34_best.pt
training/checkpoints/unet_best_prev.pt
training/checkpoints/unet_domain.pt
training/checkpoints/unet_new_aug.pt

# ===== Gambar output besar (bisa dibuat ulang: python sidebyside_all.py / zoom_all.py) =====
training/export_test/sidebyside/
training/export_test/zoom/
training/hasil/*.png
hasil/*.png
hasil/**/*.png
hasil_universal/*.png
# folder output hasil run_ecg.py / run_any.py (JSON kecil boleh, gambar tidak)

# ===== Laporan & ilustrasi (DOKUMENTASI lokal; tim cukup baca kode + hasil JSON) =====
laporan_ekg.html
lap_img/
metrik_hasil.json

# ===== Python =====
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# ===== OS / editor =====
.DS_Store
Thumbs.db
.vscode/
.idea/

# CATATAN: data pasien (Export/ PDF + JSON hasil) SENGAJA ikut di-push
# (pilihan pemilik repo; PDF sudah de-identified: hanya ID urut + sex).
